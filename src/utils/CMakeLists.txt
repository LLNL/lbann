add_sources(
  quantizer.cpp
  summary.cpp
  random.cpp
  statistics.cpp
  graph.cpp
  im2col.cpp
  cudnn_wrapper.cpp
  options.cpp
  omp_diagnostics.cpp
  protobuf_utils.cpp
  stack_trace.cpp
)

if(LBANN_HAS_CUDA)
  add_sources(
    cudnn_wrapper.cu
    )
endif()
